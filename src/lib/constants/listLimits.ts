/** Dashboard preview cards — keep in sync with DashboardActivity. */
export const DASHBOARD_PREVIEW_LIMIT = 3

/** Full-page lists: initial batch + each "Load more" step. */
export const ACTIVITY_PAGE_SIZE = 20
export const EXPENSE_PAGE_SIZE = 15
export const NOTIFICATION_PAGE_SIZE = 25

/** Group detail sidebar caps. */
export const MEMBER_SCROLL_THRESHOLD = 8
export const MY_ACTIONS_PREVIEW = 5
